{
  "name": "@inference-gateway/adk-example-artifacts-filesystem",
  "version": "0.0.0",
  "private": true,
  "description": "Filesystem-backed artifact example for @inference-gateway/adk - persists artifacts to disk via FilesystemArtifactStorage and serves them through the built-in /artifacts download endpoint.",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "start:server": "tsx server.ts",
    "start:client": "tsx client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inference-gateway/adk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
