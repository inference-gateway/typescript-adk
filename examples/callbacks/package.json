{
  "name": "@inference-gateway/adk-example-callbacks",
  "version": "0.0.0",
  "private": true,
  "description": "Callback lifecycle hooks example for @inference-gateway/adk — caching, guardrail, and audit-logging demos around a fake LLM.",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "start:server": "tsx server.ts",
    "start:client": "tsx client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inference-gateway/adk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
