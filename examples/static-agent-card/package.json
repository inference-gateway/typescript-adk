{
  "name": "@inference-gateway/adk-example-static-agent-card",
  "version": "0.0.0",
  "private": true,
  "description": "Static agent card example for @inference-gateway/adk - loads agent metadata from a JSON file with build-time placeholder substitution.",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "start:server": "tsx server.ts",
    "start:client": "tsx client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inference-gateway/adk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
