{
  "name": "@inference-gateway/adk-example-artifacts-with-default-handlers",
  "version": "0.0.0",
  "private": true,
  "description": "Combines A2AServerBuilder.withDefaultTaskHandlers() with artifact wiring (withArtifactService + a custom artifact-attaching background handler) for @inference-gateway/adk.",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "start:server": "tsx server.ts",
    "start:client": "tsx client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inference-gateway/adk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
